# macOS / editors / Jupyter
.DS_Store
.vscode/
.ipynb_checkpoints/
.notebook_dir/

# Python
__pycache__/
*.py[cod]
*.so

# Virtual environments
venv/
venv311/
.venv/

# Logs
*.log

# Streamlit cache
.streamlit/

# --- Data: ignore heavy data by default ---
data/raw/**
data/clean/**

# Keep readmes (so folders exist in Git)
!data/raw/README.md
!data/clean/README.md

# Optional: keep tiny samples (create these folders for small demo files)
!data/raw/samples/**
!data/clean/samples/**

# Model artifacts (ignored by default)
*.pkl
*.joblib
*.onnx

# OPTIONAL: keep a specific small model (uncomment if needed and <100MB)
#!data/clean/electricity_model.pkl

# AWS SageMaker artifacts
*.tar.gz
xgboost-model
model.tar.gz
train.csv
test.csv
sagemaker_train_full.csv
sagemaker_test_full.csv

# Model comparison exports (optional - can commit these if small)
# model_comparison_results.csv
# all_models_detailed.csv

# Parquet files (can be very large)
*.parquet